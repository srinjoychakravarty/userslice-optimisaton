ranked_list = []
cgtop = `systemd-cgtop -b -c --iterations=2 --delay=1`
lines = cgtop.lines()
for x = 99; x < lines.len(); x = x + 1 
{
   line = lines[x]
   raw_array = line.split()
   line_specs = []
    for element in raw_array 
    {
        if (element != "" && element != "-") 
        {
            echo(element)
            line_specs[line_specs.len()] = element
        }
    }
    ranked_list[ranked_list.len()] = line_specs
}
